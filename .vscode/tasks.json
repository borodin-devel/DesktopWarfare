{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build DesktopWarfare",
            "type": "process",
            "command": "cmake",
            "args": [
                "--build", "${workspaceFolder}/out/build/${command:cmake.activeBuildPresetName}",
                "--parallel", "8"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$msCompile"]
        }
    ]
}
